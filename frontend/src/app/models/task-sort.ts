export enum TaskSort {
  CODE = 'code',
  PRIORITY = 'priority',
  STATUS = 'status',
  CREATED = 'created',
  UPDATED = 'updated',
  DUE_DATE = 'due date',
  ESTIMATION = 'estimation',
  ASSIGNEE = 'assignee'
}
