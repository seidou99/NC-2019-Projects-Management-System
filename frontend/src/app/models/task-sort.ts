export enum TaskSort {
  TASK = 'task',
  PRIORITY = 'priority',
  STATUS = 'status',
  CREATED = 'created',
  UPDATED = 'updated',
  DUE_DATE = 'due date',
  ESTIMATION = 'estimation',
  ASSIGNEE = 'assignee'
}
